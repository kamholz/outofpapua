export const table = 'source';

export const allowed = new Set(['language_id', 'note', 'reference', 'reference_full', 'title']);
export const required = new Set(['language_id', 'reference', 'title']);
export const nfc = new Set(['reference', 'title']);
